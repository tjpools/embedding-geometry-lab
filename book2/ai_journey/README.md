# AI Journey

This crate reconstructs the conceptual lineage from symbolic and probabilistic AI through neural sequence modeling, attention, and the transformer.

Module order:

`symbolic → probabilistic → neural → sequence → attention → transformer`

The lineage is historical and conceptual, not a claim that the transformer subsumes every earlier paradigm.