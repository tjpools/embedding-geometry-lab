# Mathematics Journey

This crate follows the mathematical dependencies that make transformer architectures expressible and trainable.

Module order:

`algebra → vectors → matrices → probability → calculus → optimization → tensors → geometry`

The order is explanatory rather than a strict chronology. Later modules depend on structures introduced earlier, while several developed historically in parallel.